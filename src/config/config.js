import { theme, darkTheme } from '../presentation/common/Theme/themeVariables';

const config = {
  darkMode: false,
  topMenu: false,
  rtl: false,
  theme,
  darkTheme,
  auth0: true,
};

export default config;
